---
title: CLI
description: LotionCode CLI options and commands.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

The LotionCode CLI by default starts the [TUI](/docs/tui) when run without any arguments.

```bash
lotioncode
```

But it also accepts commands as documented on this page. This allows you to interact with LotionCode programmatically.

```bash
lotioncode run "Explain how closures work in JavaScript"
```

---

### tui

Start the LotionCode terminal user interface.

```bash
lotioncode [project]
```

#### Flags

| Flag         | Short | Description                                |
| ------------ | ----- | ------------------------------------------ |
| `--continue` | `-c`  | Continue the last session                  |
| `--session`  | `-s`  | Session ID to continue                     |
| `--prompt`   |       | Prompt to use                              |
| `--model`    | `-m`  | Model to use in the form of provider/model |
| `--agent`    |       | Agent to use                               |
| `--port`     |       | Port to listen on                          |
| `--hostname` |       | Hostname to listen on                      |

---

## Commands

The LotionCode CLI also has the following commands.

---

### agent

Manage agents for LotionCode.

```bash
lotioncode agent [command]
```

---

### attach

Attach a terminal to an already running LotionCode backend server started via `serve` or `web` commands.

```bash
lotioncode attach [url]
```

This allows using the TUI with a remote LotionCode backend. For example:

```bash
# Start the backend server for web/mobile access
lotioncode web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
lotioncode attach http://10.20.30.40:4096
```

#### Flags

| Flag        | Short | Description                       |
| ----------- | ----- | --------------------------------- |
| `--dir`     |       | Working directory to start TUI in |
| `--session` | `-s`  | Session ID to continue            |

---

#### create

Create a new agent with custom configuration.

```bash
lotioncode agent create
```

This command will guide you through creating a new agent with a custom system prompt and tool configuration.

---

#### list

List all available agents.

```bash
lotioncode agent list
```

---

### auth

Command to manage credentials and login for providers.

```bash
lotioncode auth [command]
```

---

#### login

LotionCode is powered by the provider list at [Models.dev](https://models.dev), so you can use `lotioncode auth login` to configure API keys for any provider you'd like to use. This is stored in `~/.local/share/lotioncode/auth.json`.

```bash
lotioncode auth login
```

When LotionCode starts up it loads the providers from the credentials file. And if there are any keys defined in your environments or a `.env` file in your project.

---

#### list

Lists all the authenticated providers as stored in the credentials file.

```bash
lotioncode auth list
```

Or the short version.

```bash
lotioncode auth ls
```

---

#### logout

Logs you out of a provider by clearing it from the credentials file.

```bash
lotioncode auth logout
```

---

### github

Manage the GitHub agent for repository automation.

```bash
lotioncode github [command]
```

---

#### install

Install the GitHub agent in your repository.

```bash
lotioncode github install
```

This sets up the necessary GitHub Actions workflow and guides you through the configuration process. [Learn more](/docs/github).

---

#### run

Run the GitHub agent. This is typically used in GitHub Actions.

```bash
lotioncode github run
```

##### Flags

| Flag      | Description                            |
| --------- | -------------------------------------- |
| `--event` | GitHub mock event to run the agent for |
| `--token` | GitHub personal access token           |

---

### mcp

Manage Model Context Protocol servers.

```bash
lotioncode mcp [command]
```

---

#### add

Add an MCP server to your configuration.

```bash
lotioncode mcp add
```

This command will guide you through adding either a local or remote MCP server.

---

#### list

List all configured MCP servers and their connection status.

```bash
lotioncode mcp list
```

Or use the short version.

```bash
lotioncode mcp ls
```

---

#### auth

Authenticate with an OAuth-enabled MCP server.

```bash
lotioncode mcp auth [name]
```

If you don't provide a server name, you'll be prompted to select from available OAuth-capable servers.

You can also list OAuth-capable servers and their authentication status.

```bash
lotioncode mcp auth list
```

Or use the short version.

```bash
lotioncode mcp auth ls
```

---

#### logout

Remove OAuth credentials for an MCP server.

```bash
lotioncode mcp logout [name]
```

---

#### debug

Debug OAuth connection issues for an MCP server.

```bash
lotioncode mcp debug <name>
```

---

### models

List all available models from configured providers.

```bash
lotioncode models [provider]
```

This command displays all models available across your configured providers in the format `provider/model`.

This is useful for figuring out the exact model name to use in [your config](/docs/config/).

You can optionally pass a provider ID to filter models by that provider.

```bash
lotioncode models anthropic
```

#### Flags

| Flag        | Description                                                  |
| ----------- | ------------------------------------------------------------ |
| `--refresh` | Refresh the models cache from models.dev                     |
| `--verbose` | Use more verbose model output (includes metadata like costs) |

Use the `--refresh` flag to update the cached model list. This is useful when new models have been added to a provider and you want to see them in LotionCode.

```bash
lotioncode models --refresh
```

---

### run

Run lotioncode in non-interactive mode by passing a prompt directly.

```bash
lotioncode run [message..]
```

This is useful for scripting, automation, or when you want a quick answer without launching the full TUI. For example.

```bash "lotioncode run"
lotioncode run Explain the use of context in Go
```

You can also attach to a running `lotioncode serve` instance to avoid MCP server cold boot times on every run:

```bash
# Start a headless server in one terminal
lotioncode serve

# In another terminal, run commands that attach to it
lotioncode run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Flags

| Flag         | Short | Description                                                        |
| ------------ | ----- | ------------------------------------------------------------------ |
| `--command`  |       | The command to run, use message for args                           |
| `--continue` | `-c`  | Continue the last session                                          |
| `--session`  | `-s`  | Session ID to continue                                             |
| `--share`    |       | Share the session                                                  |
| `--model`    | `-m`  | Model to use in the form of provider/model                         |
| `--agent`    |       | Agent to use                                                       |
| `--file`     | `-f`  | File(s) to attach to message                                       |
| `--format`   |       | Format: default (formatted) or json (raw JSON events)              |
| `--title`    |       | Title for the session (uses truncated prompt if no value provided) |
| `--attach`   |       | Attach to a running lotioncode server (e.g., http://localhost:4096)  |
| `--port`     |       | Port for the local server (defaults to random port)                |

---

### serve

Start a headless LotionCode server for API access. Check out the [server docs](/docs/server) for the full HTTP interface.

```bash
lotioncode serve
```

This starts an HTTP server that provides API access to lotioncode functionality without the TUI interface. Set `LOTIONCODE_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `lotioncode`).

#### Flags

| Flag         | Description                                |
| ------------ | ------------------------------------------ |
| `--port`     | Port to listen on                          |
| `--hostname` | Hostname to listen on                      |
| `--mdns`     | Enable mDNS discovery                      |
| `--cors`     | Additional browser origin(s) to allow CORS |

---

### session

Manage LotionCode sessions.

```bash
lotioncode session [command]
```

---

#### list

List all LotionCode sessions.

```bash
lotioncode session list
```

##### Flags

| Flag          | Short | Description                          |
| ------------- | ----- | ------------------------------------ |
| `--max-count` | `-n`  | Limit to N most recent sessions      |
| `--format`    |       | Output format: table or json (table) |

---

### stats

Show token usage and cost statistics for your LotionCode sessions.

```bash
lotioncode stats
```

#### Flags

| Flag        | Description                                                                 |
| ----------- | --------------------------------------------------------------------------- |
| `--days`    | Show stats for the last N days (all time)                                   |
| `--tools`   | Number of tools to show (all)                                               |
| `--models`  | Show model usage breakdown (hidden by default). Pass a number to show top N |
| `--project` | Filter by project (all projects, empty string: current project)             |

---

### export

Export session data as JSON.

```bash
lotioncode export [sessionID]
```

If you don't provide a session ID, you'll be prompted to select from available sessions.

---

### import

Import session data from a JSON file or LotionCode share URL.

```bash
lotioncode import <file>
```

You can import from a local file or an LotionCode share URL.

```bash
lotioncode import session.json
lotioncode import https://opncd.ai/s/abc123
```

---

### web

Start a headless LotionCode server with a web interface.

```bash
lotioncode web
```

This starts an HTTP server and opens a web browser to access LotionCode through a web interface. Set `LOTIONCODE_SERVER_PASSWORD` to enable HTTP basic auth (username defaults to `lotioncode`).

#### Flags

| Flag         | Description                                |
| ------------ | ------------------------------------------ |
| `--port`     | Port to listen on                          |
| `--hostname` | Hostname to listen on                      |
| `--mdns`     | Enable mDNS discovery                      |
| `--cors`     | Additional browser origin(s) to allow CORS |

---

### acp

Start an ACP (Agent Client Protocol) server.

```bash
lotioncode acp
```

This command starts an ACP server that communicates via stdin/stdout using nd-JSON.

#### Flags

| Flag         | Description           |
| ------------ | --------------------- |
| `--cwd`      | Working directory     |
| `--port`     | Port to listen on     |
| `--hostname` | Hostname to listen on |

---

### uninstall

Uninstall LotionCode and remove all related files.

```bash
lotioncode uninstall
```

#### Flags

| Flag            | Short | Description                                 |
| --------------- | ----- | ------------------------------------------- |
| `--keep-config` | `-c`  | Keep configuration files                    |
| `--keep-data`   | `-d`  | Keep session data and snapshots             |
| `--dry-run`     |       | Show what would be removed without removing |
| `--force`       | `-f`  | Skip confirmation prompts                   |

---

### upgrade

Updates lotioncode to the latest version or a specific version.

```bash
lotioncode upgrade [target]
```

To upgrade to the latest version.

```bash
lotioncode upgrade
```

To upgrade to a specific version.

```bash
lotioncode upgrade v0.1.48
```

#### Flags

| Flag       | Short | Description                                                       |
| ---------- | ----- | ----------------------------------------------------------------- |
| `--method` | `-m`  | The installation method that was used; curl, npm, pnpm, bun, brew |

---

## Global Flags

The lotioncode CLI takes the following global flags.

| Flag           | Short | Description                          |
| -------------- | ----- | ------------------------------------ |
| `--help`       | `-h`  | Display help                         |
| `--version`    | `-v`  | Print version number                 |
| `--print-logs` |       | Print logs to stderr                 |
| `--log-level`  |       | Log level (DEBUG, INFO, WARN, ERROR) |

---

## Environment variables

LotionCode can be configured using environment variables.

| Variable                              | Type    | Description                                       |
| ------------------------------------- | ------- | ------------------------------------------------- |
| `LOTIONCODE_AUTO_SHARE`                 | boolean | Automatically share sessions                      |
| `LOTIONCODE_GIT_BASH_PATH`              | string  | Path to Git Bash executable on Windows            |
| `LOTIONCODE_CONFIG`                     | string  | Path to config file                               |
| `LOTIONCODE_CONFIG_DIR`                 | string  | Path to config directory                          |
| `LOTIONCODE_CONFIG_CONTENT`             | string  | Inline json config content                        |
| `LOTIONCODE_DISABLE_AUTOUPDATE`         | boolean | Disable automatic update checks                   |
| `LOTIONCODE_DISABLE_PRUNE`              | boolean | Disable pruning of old data                       |
| `LOTIONCODE_DISABLE_TERMINAL_TITLE`     | boolean | Disable automatic terminal title updates          |
| `LOTIONCODE_PERMISSION`                 | string  | Inlined json permissions config                   |
| `LOTIONCODE_DISABLE_DEFAULT_PLUGINS`    | boolean | Disable default plugins                           |
| `LOTIONCODE_DISABLE_LSP_DOWNLOAD`       | boolean | Disable automatic LSP server downloads            |
| `LOTIONCODE_ENABLE_EXPERIMENTAL_MODELS` | boolean | Enable experimental models                        |
| `LOTIONCODE_DISABLE_AUTOCOMPACT`        | boolean | Disable automatic context compaction              |
| `LOTIONCODE_DISABLE_CLAUDE_CODE`        | boolean | Disable reading from `.claude` (prompt + skills)  |
| `LOTIONCODE_DISABLE_CLAUDE_CODE_PROMPT` | boolean | Disable reading `~/.claude/CLAUDE.md`             |
| `LOTIONCODE_DISABLE_CLAUDE_CODE_SKILLS` | boolean | Disable loading `.claude/skills`                  |
| `LOTIONCODE_CLIENT`                     | string  | Client identifier (defaults to `cli`)             |
| `LOTIONCODE_ENABLE_EXA`                 | boolean | Enable Exa web search tools                       |
| `LOTIONCODE_SERVER_PASSWORD`            | string  | Enable basic auth for `serve`/`web`               |
| `LOTIONCODE_SERVER_USERNAME`            | string  | Override basic auth username (default `lotioncode`) |

---

### Experimental

These environment variables enable experimental features that may change or be removed.

| Variable                                        | Type    | Description                             |
| ----------------------------------------------- | ------- | --------------------------------------- |
| `LOTIONCODE_EXPERIMENTAL`                         | boolean | Enable all experimental features        |
| `LOTIONCODE_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Enable icon discovery                   |
| `LOTIONCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | Disable copy on select in TUI           |
| `LOTIONCODE_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | Default timeout for bash commands in ms |
| `LOTIONCODE_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | Max output tokens for LLM responses     |
| `LOTIONCODE_EXPERIMENTAL_FILEWATCHER`             | boolean | Enable file watcher for entire dir      |
| `LOTIONCODE_EXPERIMENTAL_OXFMT`                   | boolean | Enable oxfmt formatter                  |
| `LOTIONCODE_EXPERIMENTAL_LSP_TOOL`                | boolean | Enable experimental LSP tool            |
| `LOTIONCODE_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Disable file watcher                    |
| `LOTIONCODE_EXPERIMENTAL_EXA`                     | boolean | Enable experimental Exa features        |
| `LOTIONCODE_EXPERIMENTAL_LSP_TY`                  | boolean | Enable experimental LSP type checking   |
| `LOTIONCODE_EXPERIMENTAL_MARKDOWN`                | boolean | Enable experimental markdown features   |
| `LOTIONCODE_EXPERIMENTAL_PLAN_MODE`               | boolean | Enable plan mode                        |
