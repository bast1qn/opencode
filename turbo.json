{
  "$schema": "https://turborepo.com/schema.json",
  "globalEnv": ["CI", "LOTIONCODE_DISABLE_SHARE"],
  "globalPassThroughEnv": ["CI", "LOTIONCODE_DISABLE_SHARE"],
  "tasks": {
    "typecheck": {},
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lotioncode#test": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "@lotioncode-ai/app#test": {
      "dependsOn": ["^build"],
      "outputs": []
    }
  }
}
